beforeAll(async () => {
  // is run before all tests
})

afterAll(async () => {
  // is run after all tests
})

beforeEach(async () => {
  // is run before each test
})

afterEach(async () => {
  // is run after each test
})
