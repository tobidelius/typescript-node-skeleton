npm i -D typescript \
  @tsconfig/node24 \
  @types/node \
  @vitest/coverage-v8 \
  vitest \
  @biomejs/biome

if [[ -f .github/workflows/ci.yml ]]; then
  sed -i 's|run: ./setup # change this to: npm install|run: npm install|' .github/workflows/ci.yml
fi
